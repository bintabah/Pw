<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Log in!{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

    <!-- Custom Styles -->
    <style>
        body {
            background-color: #f8f9fa; /* Fond gris clair */
        }

        .container {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .card {
            width: 18rem;
            background-color: #ffffff; /* Fond blanc */
            color: #495057; /* Couleur de texte plus foncée */
            border-radius: 10px;
            padding: 30px;
            margin-top: 50px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Ombre légère */
        }

        .form-control {
            background-color: #ffffff; /* Fond blanc pour les champs de formulaire */
            color: #495057; /* Couleur de texte plus foncée */
            border: 1px solid #ced4da; /* Bordure légère */
            margin-bottom: 10px;
        }

        .btn-success {
            background-color: #28a745; /* Vert Bootstrap pour le bouton */
            border-color: #28a745; /* Bordure de couleur verte */
            width: 100%;
            padding: 10px; /* Ajustement de la taille du bouton */
            font-size: 16px; /* Ajustement de la taille du texte du bouton */
            border-radius: 5px; /* Bords plus arrondis */
        }

        .btn-success:hover {
            background-color: #218838; /* Vert Bootstrap plus foncé au survol du bouton */
            border-color: #218838; /* Bordure de couleur verte plus foncée */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %}

                <h1 class="h3 mb-3 font-weight-normal">Saisir vos informations</h1>
                <label for="inputEmail">Email</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                <label for="inputPassword">Password</label>
                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                <button class="btn btn-lg btn-success" type="submit">Sign in</button>
            </form>

            <div class="mt-4 p-3" style="background:#e8f4fd; border:1px solid #bee3f8; border-radius:6px; font-size:0.875rem; color:#374151;">
                <p style="font-weight:600; color:#1d6fa4; margin-bottom:6px;">Accès démo</p>
                <p style="margin:2px 0;"><strong>Admin :</strong> pierre.moreau@club.fr</p>
                <p style="margin:2px 0;"><strong>Mot de passe :</strong> admin123</p>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (optional, but may be needed for some components) -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
</body>
</html>
